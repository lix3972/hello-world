1、标题和换行(仅*.md文件)
  编辑状态下，对于.md文件，# (#后有一个空格)，后面的文字变为标题，#的多少对应标题的大小，#越多，标题越小。非md文件则无此作用。
  编辑状态下，行尾部敲两个空格，显示时，会换行。非md文件则不必。
2、添加文件夹  
  在新建文件时，在新建文件名后加/。
3、文件中添加图片
  文件必须是.md后缀
  新建picture文件夹(建议不是必须)，进入picture文件夹，上传图片文件，可以一次选多个。
  点开图片，复制图片对应的网址
  在需要添加图片的行写 ![](),[]中内容自填方便自己阅读，()中填入图片文件的网址。
  例如：“![formula1](https://github.com/lix3972/papers/blob/master/CycleGan/picture/Lgan.png)”
